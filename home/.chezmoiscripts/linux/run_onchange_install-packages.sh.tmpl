#!/bin/sh

sudo apt update

sudo apt install -y btop bzip2 curl detox git htop fish fzf jq mc mplayer moreutils \
       myrepos ncdu neofetch nmap perl pipx python3 python3-pip starship \
       tcpdump tcpreplay terminator tldr tmux tree unzip vcsh vim zip  zoxide zsh

{{ if .desktop -}}

sudo apt install -y copyq

{{- end }}
