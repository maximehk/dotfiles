for bindir in "$HOME/.local/bin" "$HOME/bin"
do
  if [ -d "${bindir}" ]; then
    export PATH="${bindir}:$PATH"
  fi
done


[ -f /Users/maximeh/.local/share/google-cloud-sdk/path.zsh.inc ] && source /Users/maximeh/.local/share/google-cloud-sdk/path.zsh.inc
[ -f /Users/maximeh/.local/share/google-cloud-sdk/completion.zsh.inc ] && source /Users/maximeh/.local/share/google-cloud-sdk/completion.zsh.inc


{{-   if eq .chezmoi.os "darwin" }}

chrome='open -a '\''Google Chrome'\'
export LESS=eFRX
{{- else }}
export LESS='-S'          
{{- end }}

export LC_CTYPE="en_US.UTF-8"
export LC_ALL="en_US.UTF-8"

export EDITOR='vim'
